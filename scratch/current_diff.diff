diff --git a/data/chroma/45a5a7bf-4594-4d5b-9e0d-e0f0264d8965/data_level0.bin b/data/chroma/45a5a7bf-4594-4d5b-9e0d-e0f0264d8965/data_level0.bin
index 06d64d7..424200c 100644
Binary files a/data/chroma/45a5a7bf-4594-4d5b-9e0d-e0f0264d8965/data_level0.bin and b/data/chroma/45a5a7bf-4594-4d5b-9e0d-e0f0264d8965/data_level0.bin differ
diff --git a/data/chroma/45a5a7bf-4594-4d5b-9e0d-e0f0264d8965/length.bin b/data/chroma/45a5a7bf-4594-4d5b-9e0d-e0f0264d8965/length.bin
index 57ec86c..0e5e879 100644
Binary files a/data/chroma/45a5a7bf-4594-4d5b-9e0d-e0f0264d8965/length.bin and b/data/chroma/45a5a7bf-4594-4d5b-9e0d-e0f0264d8965/length.bin differ
diff --git a/data/chroma/94e4a397-19c7-4012-a478-9cf39919f39d/data_level0.bin b/data/chroma/94e4a397-19c7-4012-a478-9cf39919f39d/data_level0.bin
index 84ef7d1..accbe91 100644
Binary files a/data/chroma/94e4a397-19c7-4012-a478-9cf39919f39d/data_level0.bin and b/data/chroma/94e4a397-19c7-4012-a478-9cf39919f39d/data_level0.bin differ
diff --git a/data/chroma/94e4a397-19c7-4012-a478-9cf39919f39d/length.bin b/data/chroma/94e4a397-19c7-4012-a478-9cf39919f39d/length.bin
index b495e14..0b7b581 100644
Binary files a/data/chroma/94e4a397-19c7-4012-a478-9cf39919f39d/length.bin and b/data/chroma/94e4a397-19c7-4012-a478-9cf39919f39d/length.bin differ
diff --git a/data/chroma/acef97e2-cb71-4ccd-99d4-4389abbfd3d4/data_level0.bin b/data/chroma/acef97e2-cb71-4ccd-99d4-4389abbfd3d4/data_level0.bin
index 5c818a7..a60f5c0 100644
Binary files a/data/chroma/acef97e2-cb71-4ccd-99d4-4389abbfd3d4/data_level0.bin and b/data/chroma/acef97e2-cb71-4ccd-99d4-4389abbfd3d4/data_level0.bin differ
diff --git a/data/chroma/acef97e2-cb71-4ccd-99d4-4389abbfd3d4/length.bin b/data/chroma/acef97e2-cb71-4ccd-99d4-4389abbfd3d4/length.bin
index 9393812..29ad17e 100644
Binary files a/data/chroma/acef97e2-cb71-4ccd-99d4-4389abbfd3d4/length.bin and b/data/chroma/acef97e2-cb71-4ccd-99d4-4389abbfd3d4/length.bin differ
diff --git a/data/chroma/chroma.sqlite3 b/data/chroma/chroma.sqlite3
index 7e2c007..2e8ce4e 100644
Binary files a/data/chroma/chroma.sqlite3 and b/data/chroma/chroma.sqlite3 differ
diff --git a/src/jarvis/llm/engine.py b/src/jarvis/llm/engine.py
index 9819297..4870ade 100644
--- a/src/jarvis/llm/engine.py
+++ b/src/jarvis/llm/engine.py
@@ -201,41 +201,7 @@ class LLMEngine:
                 context = "\n\n".join(context_parts)
                 log.info("RAG: Recobrados {} chunks relevantes do VectorStore.", len(context_parts))
                 
-            # 1b. Recupera trechos de código do RAG de Código Fonte
-            code_results = await self._vector_store.query_collection(
-                collection_name="code_snippets",
-                query_embeddings=[query_emb],
-                limit=2,
-            )
-            code_parts = []
-            for r in code_results:
-                if r.get("distance", 1.0) < 0.85:
-                    meta = r.get("metadata", {})
-                    source = meta.get("source", "Desconhecido")
-                    etype = meta.get("type", "Desconhecido")
-                    name = meta.get("name", "Desconhecido")
-                    start_l = meta.get("start_line", 0)
-                    end_l = meta.get("end_line", 0)
-                    
-                    code_content = r['document']
-                    if len(code_content) > 800:
-                        code_content = code_content[:800] + "\n... [Código truncado para economizar contexto]"
-                    
-                    code_parts.append(
-                        f"Arquivo: {source}\n"
-                        f"Tipo: {etype}\n"
-                        f"Nome: {name}\n"
-                        f"Linhas: {start_l}-{end_l}\n"
-                        f"Conteúdo:\n{code_content}"
-                    )
-            
-            if code_parts:
-                code_context = "\n\n========================================\n\n".join(code_parts)
-                if context:
-                    context += "\n\n=== CONTEXTO DE CÓDIGO FONTE DO PROJETO ===\n\n" + code_context
-                else:
-                    context = "=== CONTEXTO DE CÓDIGO FONTE DO PROJETO ===\n\n" + code_context
-                log.info("RAG: Recobrados {} snippets de código relevantes.", len(code_parts))
+
                 
             # 2. Recupera turnos de conversa anteriores em ordem cronológica (últimos 3 turnos)
             conv_parts = await self._vector_store.get_chronological_conversations(limit=3)
diff --git a/src/jarvis/main.py b/src/jarvis/main.py
index cdc85cf..b767238 100644
--- a/src/jarvis/main.py
+++ b/src/jarvis/main.py
@@ -399,6 +399,7 @@ async def run_stt_loop() -> None:
     ) -> None:
         web.llm_generating = True
         web.llm_task = asyncio.current_task()
+        web.llm_interrupted_by_voice = False
         log.info("PERF: Iniciando geração do LLM para o prompt: '{}' (Origem: {})", prompt_text, origin)
         console.print(f"🤖 [bold cyan]Jarvis [{origin}]:[/bold cyan] ", end="")
         
@@ -410,6 +411,8 @@ async def run_stt_loop() -> None:
         first_token_received = False
         try:
             async for token in llm.generate_stream(prompt_text, language=lang, ha_client=ha_client):
+                if llm._is_cancelled or web.llm_interrupted_by_voice:
+                    raise asyncio.CancelledError()
                 if not first_token_received:
                     log.info("PERF: Primeiro token verbal recebido do LLM.")
                     first_token_received = True
@@ -442,6 +445,9 @@ async def run_stt_loop() -> None:
                         if clean_s:
                             web.browser_recent_texts.append((time.time(), clean_s))
 
+            if llm._is_cancelled or web.llm_interrupted_by_voice:
+                raise asyncio.CancelledError()
+
             # Envia a última parte restante do buffer ao finalizar a geração
             remaining_sentence = sentence_buffer.strip()
             if remaining_sentence:
@@ -490,6 +496,7 @@ async def run_stt_loop() -> None:
             sys.stdout.write("\n\n")
             sys.stdout.flush()
             web.llm_generating = False
+            web.llm_interrupted_by_voice = False
             await web.broadcast_chat_status("idle")
 
     # Vincula o callback para que web.py possa disparar respostas no mesmo fluxo
@@ -572,17 +579,21 @@ async def run_stt_loop() -> None:
                                     is_echo = False
                                     current_spoken = tts.current_spoken_text
                                     if current_spoken:
-                                        # 1. Correspondência direta de substring
-                                        if final_text_clean in current_spoken or current_spoken in final_text_clean:
-                                            is_echo = True
-                                        # 2. Correspondência de interseção de palavras
-                                        else:
-                                            words_trans = set(final_text_clean.split())
-                                            words_spok = set(current_spoken.split())
-                                            if words_trans and words_spok:
-                                                intersection = words_trans.intersection(words_spok)
-                                                if len(intersection) / len(words_trans) > 0.6:
-                                                    is_echo = True
+                                        # Bypass AEC if the user is clearly speaking a stop command (to handle barge-in while speaking)
+                                        has_stop_word = any(word in final_text_clean for word in ("pare", "parar", "cala a boca", "silêncio", "quieto", "stop", "shut up", "be quiet", "silence", "pera", "espera", "calma", "chega", "shh", "shush"))
+                                        
+                                        if not has_stop_word:
+                                            # 1. Correspondência direta de substring
+                                            if final_text_clean in current_spoken or current_spoken in final_text_clean:
+                                                is_echo = True
+                                            # 2. Correspondência de interseção de palavras
+                                            else:
+                                                words_trans = set(final_text_clean.split())
+                                                words_spok = set(current_spoken.split())
+                                                if words_trans and words_spok:
+                                                    intersection = words_trans.intersection(words_spok)
+                                                    if len(intersection) / len(words_trans) > 0.6:
+                                                        is_echo = True
 
                                     if is_echo:
                                         log.info("AEC: Transcrição de eco ignorada: '{}'", final_text)
@@ -744,6 +755,11 @@ async def run_stt_loop() -> None:
                     # Concatena o áudio acumulado até o momento
                     partial_audio = np.concatenate(audio_buffer, axis=0).flatten()
 
+                    # Slice the last 1.5 seconds of audio to avoid Whisper queue lag and keep transcriptions instant
+                    max_samples = int(settings.audio.sample_rate * 1.5)
+                    if len(partial_audio) > max_samples:
+                        partial_audio = partial_audio[-max_samples:]
+
                     # Transcreve em segundo plano
                     # Criamos uma task para não bloquear o loop de captura de áudio
                     async def transcribe_partial(audio_data: np.ndarray) -> None:
@@ -762,8 +778,26 @@ async def run_stt_loop() -> None:
                                             cleaned_text = cleaned_text.replace(p, "")
                                         cleaned_text = cleaned_text.strip()
 
-                                        stop_words = ("jarvis", "para", "pare", "parar", "cala a boca", "silêncio", "quieto", "stop", "shut up", "be quiet", "silence", "pera", "espera", "calma", "chega", "shh", "shush")
+                                        stop_words = ("pare", "parar", "cala a boca", "silêncio", "quieto", "stop", "shut up", "be quiet", "silence", "pera", "espera", "calma", "chega", "shh", "shush")
                                         if any(word in cleaned_text for word in stop_words):
+                                            # AEC check for terminal partial transcription
+                                            is_echo = False
+                                            current_spoken = tts.current_spoken_text
+                                            if current_spoken:
+                                                if cleaned_text in current_spoken or current_spoken in cleaned_text:
+                                                    is_echo = True
+                                                else:
+                                                    words_trans = set(cleaned_text.split())
+                                                    words_spok = set(current_spoken.split())
+                                                    if words_trans and words_spok:
+                                                        intersection = words_trans.intersection(words_spok)
+                                                        if len(intersection) / len(words_trans) > 0.6:
+                                                            is_echo = True
+                                            
+                                            if is_echo:
+                                                log.debug("AEC Parcial: Eco ignorado no terminal: '{}'", text)
+                                                return
+                                            
                                             if web.llm_task and not web.llm_task.done():
                                                 llm.interrupt()
                                                 web.llm_task.cancel()
diff --git a/src/jarvis/ui/templates/index.html b/src/jarvis/ui/templates/index.html
index 5d16b78..a44515a 100644
--- a/src/jarvis/ui/templates/index.html
+++ b/src/jarvis/ui/templates/index.html
@@ -5,20 +5,21 @@
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Jarvis — Web Dashboard</title>
     <!-- Google Fonts -->
-    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=Outfit:wght@400;600;800&display=swap" rel="stylesheet">
+    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;600;800;900&family=Share+Tech+Mono&family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
     <style>
         :root {
-            --bg-color: #05060f;
-            --card-bg: rgba(20, 22, 45, 0.45);
-            --border-color: rgba(147, 51, 234, 0.2);
-            --primary: #9333ea;
-            --primary-glow: rgba(147, 51, 234, 0.4);
-            --secondary: #06b6d4;
-            --secondary-glow: rgba(6, 182, 212, 0.4);
-            --text-color: #f3f4f6;
-            --text-muted: #9ca3af;
-            --accent-green: #10b981;
-            --accent-red: #ef4444;
+            --bg-color: #080300;
+            --card-bg: rgba(25, 10, 2, 0.45);
+            --border-color: rgba(255, 85, 0, 0.25);
+            --primary: #ff5500;
+            --primary-glow: rgba(255, 85, 0, 0.45);
+            --secondary: #ffaa00;
+            --secondary-glow: rgba(255, 170, 0, 0.45);
+            --text-color: #ffe6d5;
+            --text-muted: #ba8f75;
+            --accent-green: #ffaa00;
+            --accent-red: #ff3333;
+            --grid-color: rgba(255, 85, 0, 0.04);
         }
 
         * {
@@ -36,8 +37,24 @@
             flex-direction: column;
             overflow-x: hidden;
             background-image: 
-                radial-gradient(circle at 10% 20%, rgba(147, 51, 234, 0.08) 0%, transparent 40%),
-                radial-gradient(circle at 90% 80%, rgba(6, 182, 212, 0.08) 0%, transparent 40%);
+                linear-gradient(var(--grid-color) 1px, transparent 1px),
+                linear-gradient(90deg, var(--grid-color) 1px, transparent 1px),
+                radial-gradient(circle at 50% 30%, rgba(255, 85, 0, 0.08) 0%, transparent 60%);
+            background-size: 30px 30px, 30px 30px, 100% 100%;
+            position: relative;
+        }
+
+        /* Scanline animation overlay */
+        body::after {
+            content: " ";
+            display: block;
+            position: fixed;
+            top: 0; left: 0; bottom: 0; right: 0;
+            background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.15) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.03), rgba(0, 255, 0, 0.01), rgba(0, 0, 255, 0.03));
+            z-index: 9999;
+            opacity: 0.08;
+            pointer-events: none;
+            background-size: 100% 4px, 6px 100%;
         }
 
         header {
@@ -47,10 +64,11 @@
             align-items: center;
             border-bottom: 1px solid var(--border-color);
             backdrop-filter: blur(12px);
-            background: rgba(5, 6, 15, 0.7);
+            background: rgba(8, 3, 0, 0.85);
             position: sticky;
             top: 0;
             z-index: 100;
+            box-shadow: 0 4px 20px rgba(255, 85, 0, 0.05);
         }
 
         .logo-container {
@@ -60,51 +78,112 @@
         }
 
         .logo-icon {
-            width: 32px;
-            height: 32px;
+            width: 34px;
+            height: 34px;
             border-radius: 50%;
-            background: linear-gradient(135deg, var(--primary), var(--secondary));
+            border: 2px dashed var(--primary);
             box-shadow: 0 0 15px var(--primary-glow);
             position: relative;
-            animation: pulse-glow 2s infinite alternate;
+            animation: rotate-hud 10s linear infinite;
+            display: flex;
+            align-items: center;
+            justify-content: center;
+        }
+
+        .logo-icon::before {
+            content: '';
+            width: 18px;
+            height: 18px;
+            border-radius: 50%;
+            border: 2px solid var(--secondary);
+            box-shadow: 0 0 8px var(--secondary-glow);
+            position: absolute;
+            animation: rotate-hud-reverse 5s linear infinite;
         }
 
-        @keyframes pulse-glow {
-            from { box-shadow: 0 0 10px var(--primary-glow); }
-            to { box-shadow: 0 0 22px var(--primary-glow), 0 0 10px var(--secondary-glow); }
+        @keyframes rotate-hud {
+            from { transform: rotate(0deg); }
+            to { transform: rotate(360deg); }
+        }
+
+        @keyframes rotate-hud-reverse {
+            from { transform: rotate(360deg); }
+            to { transform: rotate(0deg); }
         }
 
         h1 {
-            font-family: 'Outfit', sans-serif;
-            font-size: 24px;
+            font-family: 'Orbitron', sans-serif;
+            font-size: 22px;
             font-weight: 800;
-            background: linear-gradient(to right, #fff, #a78bfa);
+            background: linear-gradient(to right, #fff, var(--secondary));
             -webkit-background-clip: text;
             -webkit-text-fill-color: transparent;
+            letter-spacing: 2px;
         }
 
         .status-badge {
-            background: rgba(16, 185, 129, 0.1);
-            border: 1px solid rgba(16, 185, 129, 0.3);
-            color: var(--accent-green);
+            background: rgba(255, 85, 0, 0.1);
+            border: 1px solid rgba(255, 85, 0, 0.35);
+            color: var(--secondary);
             padding: 6px 14px;
-            border-radius: 20px;
-            font-size: 12px;
+            border-radius: 4px;
+            font-size: 11px;
+            font-family: 'Orbitron', sans-serif;
             font-weight: 600;
             display: flex;
             align-items: center;
             gap: 6px;
+            box-shadow: 0 0 10px rgba(255, 85, 0, 0.15);
+            text-transform: uppercase;
+            letter-spacing: 1px;
         }
 
         .status-dot {
             width: 8px;
             height: 8px;
             border-radius: 50%;
-            background-color: var(--accent-green);
-            box-shadow: 0 0 8px var(--accent-green);
+            background-color: var(--secondary);
+            box-shadow: 0 0 8px var(--secondary);
             animation: blink 1.5s infinite;
         }
 
+        .fluida-badge {
+            background: rgba(255, 170, 0, 0.12);
+            border: 1px solid rgba(255, 170, 0, 0.45);
+            color: #ffaa00;
+            padding: 6px 14px;
+            border-radius: 4px;
+            font-size: 11px;
+            font-family: 'Orbitron', sans-serif;
+            font-weight: 600;
+            display: flex;
+            align-items: center;
+            gap: 6px;
+            box-shadow: 0 0 10px rgba(255, 170, 0, 0.2);
+            text-transform: uppercase;
+            letter-spacing: 1px;
+            animation: pulse-border 1.5s infinite alternate;
+        }
+
+        .fluida-dot {
+            width: 8px;
+            height: 8px;
+            border-radius: 50%;
+            background-color: #ffaa00;
+            box-shadow: 0 0 8px #ffaa00;
+            animation: blink-fast 0.8s infinite;
+        }
+
+        @keyframes blink-fast {
+            0%, 100% { opacity: 0.4; }
+            50% { opacity: 1; }
+        }
+
+        @keyframes pulse-border {
+            from { border-color: rgba(255, 170, 0, 0.3); box-shadow: 0 0 6px rgba(255, 170, 0, 0.1); }
+            to { border-color: rgba(255, 170, 0, 0.7); box-shadow: 0 0 12px rgba(255, 170, 0, 0.4); }
+        }
+
         @keyframes blink {
             0%, 100% { opacity: 0.4; }
             50% { opacity: 1; }
@@ -132,13 +211,13 @@
         .chat-card {
             background: var(--card-bg);
             border: 1px solid var(--border-color);
-            border-radius: 16px;
+            border-radius: 8px;
             backdrop-filter: blur(16px);
             padding: 24px;
             display: flex;
             flex-direction: column;
             height: 550px;
-            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
+            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.5);
         }
 
         .chat-header {
@@ -151,9 +230,11 @@
         }
 
         .chat-title {
-            font-family: 'Outfit', sans-serif;
-            font-size: 18px;
+            font-family: 'Orbitron', sans-serif;
+            font-size: 16px;
             font-weight: 600;
+            letter-spacing: 1px;
+            color: var(--secondary);
         }
 
         .chat-history {
@@ -174,14 +255,14 @@
             background: transparent;
         }
         .chat-history::-webkit-scrollbar-thumb {
-            background: rgba(147, 51, 234, 0.3);
-            border-radius: 10px;
+            background: rgba(255, 85, 0, 0.3);
+            border-radius: 4px;
         }
 
         .chat-bubble {
             max-width: 75%;
             padding: 14px 18px;
-            border-radius: 16px;
+            border-radius: 8px;
             line-height: 1.5;
             font-size: 14px;
             position: relative;
@@ -194,20 +275,23 @@
         }
 
         .chat-bubble.user {
-            background: linear-gradient(135deg, rgba(6, 182, 212, 0.2), rgba(6, 182, 212, 0.05));
-            border: 1px solid rgba(6, 182, 212, 0.25);
+            background: linear-gradient(135deg, rgba(255, 170, 0, 0.12), rgba(255, 170, 0, 0.03));
+            border: 1px solid rgba(255, 170, 0, 0.3);
             align-self: flex-end;
-            border-bottom-right-radius: 4px;
+            border-bottom-right-radius: 2px;
+            box-shadow: 0 0 10px rgba(255, 170, 0, 0.05);
         }
 
         .chat-bubble.jarvis {
-            background: linear-gradient(135deg, rgba(147, 51, 234, 0.2), rgba(147, 51, 234, 0.05));
-            border: 1px solid rgba(147, 51, 234, 0.25);
+            background: linear-gradient(135deg, rgba(255, 85, 0, 0.12), rgba(255, 85, 0, 0.03));
+            border: 1px solid rgba(255, 85, 0, 0.3);
             align-self: flex-start;
-            border-bottom-left-radius: 4px;
+            border-bottom-left-radius: 2px;
+            box-shadow: 0 0 10px rgba(255, 85, 0, 0.05);
         }
 
         .bubble-meta {
+            font-family: 'Share Tech Mono', monospace;
             font-size: 10px;
             color: var(--text-muted);
             margin-top: 6px;
@@ -224,30 +308,34 @@
 
         .chat-input {
             flex: 1;
-            background: rgba(5, 6, 15, 0.6);
+            background: rgba(25, 10, 2, 0.6);
             border: 1px solid var(--border-color);
-            border-radius: 10px;
+            border-radius: 4px;
             padding: 12px 18px;
             color: var(--text-color);
             font-size: 14px;
             outline: none;
-            transition: border-color 0.3s;
+            transition: all 0.3s;
         }
 
         .chat-input:focus {
             border-color: var(--secondary);
-            box-shadow: 0 0 8px rgba(6, 182, 212, 0.2);
+            box-shadow: 0 0 10px var(--secondary-glow);
         }
 
         .send-btn {
             background: linear-gradient(135deg, var(--primary), var(--secondary));
             border: none;
-            border-radius: 10px;
-            color: #fff;
+            border-radius: 4px;
+            color: #000;
+            font-family: 'Orbitron', sans-serif;
+            font-size: 12px;
+            letter-spacing: 0.5px;
             padding: 0 20px;
-            font-weight: 600;
+            font-weight: 700;
             cursor: pointer;
             transition: opacity 0.3s, transform 0.2s;
+            box-shadow: 0 0 10px rgba(255, 85, 0, 0.2);
         }
 
         .send-btn:hover {
@@ -262,7 +350,7 @@
         .audio-control-card {
             background: var(--card-bg);
             border: 1px solid var(--border-color);
-            border-radius: 16px;
+            border-radius: 8px;
             padding: 20px;
             display: flex;
             flex-direction: column;
@@ -273,9 +361,10 @@
         .waveform-canvas {
             width: 100%;
             height: 90px;
-            background: rgba(5, 6, 15, 0.7);
-            border-radius: 10px;
-            border: 1px solid rgba(255, 255, 255, 0.05);
+            background: rgba(8, 3, 0, 0.75);
+            border-radius: 4px;
+            border: 1px solid rgba(255, 85, 0, 0.15);
+            box-shadow: inset 0 0 10px rgba(255, 85, 0, 0.1);
         }
 
         .audio-buttons {
@@ -285,11 +374,14 @@
         }
 
         .control-btn {
-            background: rgba(147, 51, 234, 0.1);
+            background: rgba(255, 85, 0, 0.08);
             border: 1px solid var(--primary);
             color: var(--text-color);
             padding: 10px 24px;
-            border-radius: 30px;
+            border-radius: 4px;
+            font-family: 'Orbitron', sans-serif;
+            font-size: 11px;
+            letter-spacing: 0.5px;
             font-weight: 600;
             cursor: pointer;
             display: flex;
@@ -299,18 +391,19 @@
         }
 
         .control-btn:hover {
-            background: var(--primary);
-            box-shadow: 0 0 15px var(--primary-glow);
+            background: rgba(255, 85, 0, 0.2);
+            box-shadow: 0 0 12px var(--primary-glow);
         }
 
         .control-btn.active-record {
             background: var(--accent-red);
             border-color: var(--accent-red);
-            box-shadow: 0 0 15px rgba(239, 68, 68, 0.4);
+            box-shadow: 0 0 15px rgba(255, 51, 51, 0.4);
+            color: #fff;
         }
 
         .control-btn.muted {
-            background: rgba(239, 68, 68, 0.2);
+            background: rgba(255, 51, 51, 0.15);
             border-color: var(--accent-red);
             color: var(--accent-red);
         }
@@ -330,22 +423,24 @@
         .sidebar-card {
             background: var(--card-bg);
             border: 1px solid var(--border-color);
-            border-radius: 16px;
+            border-radius: 8px;
             backdrop-filter: blur(16px);
             padding: 20px;
-            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
+            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.5);
         }
 
         .card-header-sidebar {
-            font-family: 'Outfit', sans-serif;
-            font-size: 16px;
+            font-family: 'Orbitron', sans-serif;
+            font-size: 14px;
             font-weight: 600;
             margin-bottom: 16px;
             padding-bottom: 8px;
-            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
+            border-bottom: 1px solid rgba(255, 85, 0, 0.15);
             display: flex;
             align-items: center;
             gap: 8px;
+            letter-spacing: 1px;
+            color: var(--secondary);
         }
 
         /* Telemetria GPU */
@@ -362,30 +457,35 @@
         }
 
         .stat-label {
-            font-size: 13px;
+            font-size: 11px;
+            font-family: 'Orbitron', sans-serif;
+            letter-spacing: 0.5px;
             color: var(--text-muted);
         }
 
         .stat-value {
-            font-size: 14px;
+            font-size: 13px;
+            font-family: 'Share Tech Mono', monospace;
             font-weight: 600;
         }
 
         .progress-bar-container {
             width: 100%;
             height: 6px;
-            background: rgba(255, 255, 255, 0.05);
-            border-radius: 3px;
+            background: rgba(255, 85, 0, 0.08);
+            border-radius: 1px;
             overflow: hidden;
             margin-top: 4px;
+            border: 1px solid rgba(255, 85, 0, 0.15);
         }
 
         .progress-bar-fill {
             height: 100%;
             background: linear-gradient(to right, var(--primary), var(--secondary));
-            border-radius: 3px;
+            border-radius: 1px;
             width: 0%;
             transition: width 0.5s ease-out;
+            box-shadow: 0 0 8px var(--primary-glow);
         }
 
         .gauge-container {
@@ -396,21 +496,23 @@
         }
 
         .mini-gauge {
-            background: rgba(5, 6, 15, 0.4);
-            border: 1px solid rgba(255, 255, 255, 0.03);
-            border-radius: 10px;
+            background: rgba(25, 10, 2, 0.35);
+            border: 1px solid rgba(255, 85, 0, 0.15);
+            border-radius: 4px;
             padding: 12px;
             display: flex;
             flex-direction: column;
             align-items: center;
             gap: 6px;
+            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
         }
 
         .gauge-val {
-            font-size: 20px;
-            font-family: 'Outfit', sans-serif;
+            font-size: 22px;
+            font-family: 'Share Tech Mono', monospace;
             font-weight: 700;
             color: var(--secondary);
+            text-shadow: 0 0 8px var(--secondary-glow);
         }
 
         /* Home Assistant Card */
@@ -424,9 +526,9 @@
         }
 
         .ha-device-item {
-            background: rgba(5, 6, 15, 0.4);
-            border: 1px solid rgba(255, 255, 255, 0.03);
-            border-radius: 10px;
+            background: rgba(25, 10, 2, 0.35);
+            border: 1px solid rgba(255, 85, 0, 0.15);
+            border-radius: 4px;
             padding: 10px 14px;
             display: flex;
             justify-content: space-between;
@@ -435,7 +537,8 @@
         }
 
         .ha-device-item:hover {
-            border-color: rgba(6, 182, 212, 0.15);
+            border-color: rgba(255, 170, 0, 0.3);
+            box-shadow: 0 0 10px rgba(255, 85, 0, 0.1);
         }
 
         .device-info {
@@ -444,11 +547,14 @@
         }
 
         .device-name {
-            font-size: 13px;
+            font-family: 'Orbitron', sans-serif;
+            font-size: 12px;
             font-weight: 600;
+            letter-spacing: 0.5px;
         }
 
         .device-entity {
+            font-family: 'Share Tech Mono', monospace;
             font-size: 10px;
             color: var(--text-muted);
             margin-top: 2px;
@@ -472,10 +578,10 @@
             position: absolute;
             cursor: pointer;
             top: 0; left: 0; right: 0; bottom: 0;
-            background-color: rgba(255, 255, 255, 0.1);
+            background-color: rgba(255, 85, 0, 0.1);
             transition: .3s;
-            border-radius: 34px;
-            border: 1px solid rgba(255, 255, 255, 0.05);
+            border-radius: 4px;
+            border: 1px solid rgba(255, 85, 0, 0.25);
         }
 
         .toggle-slider:before {
@@ -487,16 +593,18 @@
             bottom: 3px;
             background-color: var(--text-color);
             transition: .3s;
-            border-radius: 50%;
+            border-radius: 2px;
         }
 
         input:checked + .toggle-slider {
-            background-color: var(--secondary);
+            background-color: rgba(255, 170, 0, 0.3);
+            border-color: var(--secondary);
             box-shadow: 0 0 8px var(--secondary-glow);
         }
 
         input:checked + .toggle-slider:before {
             transform: translateX(16px);
+            background-color: var(--secondary);
         }
 
         @media (max-width: 900px) {
@@ -517,9 +625,15 @@
             <div class="logo-icon"></div>
             <h1>JARVIS</h1>
         </div>
-        <div class="status-badge">
-            <div class="status-dot"></div>
-            <span id="jarvis-status-txt">ONLINE</span>
+        <div class="header-badges" style="display: flex; gap: 12px; align-items: center;">
+            <div class="fluida-badge" id="fluida-badge-element" style="display: none;">
+                <div class="fluida-dot"></div>
+                <span>ESCUTA DIRETA ATIVA</span>
+            </div>
+            <div class="status-badge">
+                <div class="status-dot"></div>
+                <span id="jarvis-status-txt">ONLINE</span>
+            </div>
         </div>
     </header>
 
@@ -662,6 +776,11 @@
                     } else {
                         tempText.style.color = 'var(--secondary)';
                     }
+                    
+                    // Atualiza o indicador visual da Escuta Direta Ativa (Conversa Fluida)
+                    if (data.hasOwnProperty('conversa_fluida')) {
+                        updateConversaFluida(data.conversa_fluida);
+                    }
                 }
 
                 // Trata o status do Jarvis
@@ -706,6 +825,17 @@
             };
         }
 
+        const fluidaBadge = document.getElementById('fluida-badge-element');
+        function updateConversaFluida(isActive) {
+            if (fluidaBadge) {
+                if (isActive) {
+                    fluidaBadge.style.display = 'flex';
+                } else {
+                    fluidaBadge.style.display = 'none';
+                }
+            }
+        }
+
         function updateStatus(status) {
             if (status === 'recording') {
                 statusDot.style.backgroundColor = 'var(--accent-red)';
@@ -713,20 +843,20 @@
                 statusTxt.innerText = 'GRAVANDO';
                 statusTxt.style.color = 'var(--accent-red)';
             } else if (status === 'thinking') {
-                statusDot.style.backgroundColor = 'orange';
-                statusDot.style.boxShadow = '0 0 8px orange';
+                statusDot.style.backgroundColor = '#ffaa00';
+                statusDot.style.boxShadow = '0 0 8px #ffaa00';
                 statusTxt.innerText = 'PROCESSANDO';
-                statusTxt.style.color = 'orange';
+                statusTxt.style.color = '#ffaa00';
             } else if (status === 'speaking') {
-                statusDot.style.backgroundColor = 'var(--secondary)';
-                statusDot.style.boxShadow = '0 0 8px var(--secondary-glow)';
+                statusDot.style.backgroundColor = 'var(--primary)';
+                statusDot.style.boxShadow = '0 0 8px var(--primary-glow)';
                 statusTxt.innerText = 'FALANDO';
-                statusTxt.style.color = 'var(--secondary)';
+                statusTxt.style.color = 'var(--primary)';
             } else {
-                statusDot.style.backgroundColor = 'var(--accent-green)';
-                statusDot.style.boxShadow = '0 0 8px var(--accent-green)';
+                statusDot.style.backgroundColor = 'var(--secondary)';
+                statusDot.style.boxShadow = '0 0 8px var(--secondary)';
                 statusTxt.innerText = 'ONLINE';
-                statusTxt.style.color = 'var(--accent-green)';
+                statusTxt.style.color = 'var(--secondary)';
             }
         }
 
@@ -818,10 +948,10 @@
 
         // Desenha a linha estática por padrão
         function drawFlatLine() {
-            canvasCtx.fillStyle = 'rgba(5, 6, 15, 0.7)';
+            canvasCtx.fillStyle = 'rgba(8, 3, 0, 0.7)';
             canvasCtx.fillRect(0, 0, canvas.width, canvas.height);
             canvasCtx.lineWidth = 2;
-            canvasCtx.strokeStyle = 'rgba(147, 51, 234, 0.2)';
+            canvasCtx.strokeStyle = 'rgba(255, 85, 0, 0.25)';
             canvasCtx.beginPath();
             canvasCtx.moveTo(0, canvas.height / 2);
             canvasCtx.lineTo(canvas.width, canvas.height / 2);
@@ -839,13 +969,13 @@
             requestAnimationFrame(drawWave);
             analyser.getByteTimeDomainData(dataArray);
 
-            canvasCtx.fillStyle = 'rgba(5, 6, 15, 0.5)';
+            canvasCtx.fillStyle = 'rgba(8, 3, 0, 0.5)';
             canvasCtx.fillRect(0, 0, canvas.width, canvas.height);
             
-            // Seleciona a cor do neon baseado no status
-            let strokeColor = 'rgba(6, 182, 212, 0.8)'; // Cyan quando desmutado e gravando
+            // Seleciona a cor do neon baseado no status (estilo Jarvis HUD)
+            let strokeColor = 'rgba(255, 170, 0, 0.85)'; // Âmbar quando desmutado e gravando
             if (isMuted) {
-                strokeColor = 'rgba(239, 68, 68, 0.5)'; // Vermelho quando mutado
+                strokeColor = 'rgba(255, 51, 51, 0.5)'; // Vermelho quando mutado
             }
 
             canvasCtx.lineWidth = 3;
diff --git a/src/jarvis/ui/web.py b/src/jarvis/ui/web.py
index d1f243c..087167e 100644
--- a/src/jarvis/ui/web.py
+++ b/src/jarvis/ui/web.py
@@ -179,11 +179,24 @@ async def ws_chat_endpoint(websocket: WebSocket) -> None:
     # Loop para enviar atualizações de telemetria periódicas e ler mensagens textuais
     async def telemetry_loop():
         try:
+            import time
+            settings = get_settings()
             while websocket in active_chat_connections:
+                # Calcula se a escuta direta (conversa fluida) está ativa no momento
+                is_fluida = False
+                if tts_last_active_time > 0.0:
+                    is_fluida = (time.time() - tts_last_active_time) < (settings.audio.full_duplex_cooldown_ms / 1000)
+                
+                is_browser_playing = time.time() < (browser_tts_end_time + 0.5)
+                is_jarvis_busy = llm_generating or is_browser_playing or (tts_engine and tts_engine._is_playing) or (tts_engine and not tts_engine._queue.empty())
+                
+                conversa_fluida_active = is_fluida and not is_jarvis_busy
+
                 stats = get_gpu_stats()
                 await websocket.send_json({
                     "type": "telemetry",
-                    "data": stats
+                    "data": stats,
+                    "conversa_fluida": conversa_fluida_active
                 })
                 await asyncio.sleep(1.5)
         except Exception:
@@ -471,9 +484,14 @@ async def ws_audio_endpoint(websocket: WebSocket) -> None:
                     last_partial_time = now
                     partial_audio = np.concatenate(audio_buffer, axis=0).flatten()
                     
+                    # Slice the last 1.5 seconds of audio to avoid Whisper queue lag and keep transcriptions instant
+                    max_samples = int(16000 * 1.5)
+                    if len(partial_audio) > max_samples:
+                        partial_audio = partial_audio[-max_samples:]
+                    
                     async def transcribe_partial(audio_data: np.ndarray) -> None:
                         nonlocal partial_in_progress, audio_buffer, is_speaking, silent_chunks
-                        global llm_interrupted_by_voice, browser_tts_end_time
+                        global llm_interrupted_by_voice, browser_tts_end_time, browser_recent_texts
                         partial_in_progress = True
                         try:
                             if transcriber_engine:
@@ -487,13 +505,35 @@ async def ws_audio_endpoint(websocket: WebSocket) -> None:
                                         was_jarvis_active = llm_generating or is_browser_playing or (tts_engine and tts_engine._is_playing) or (tts_engine and not tts_engine._queue.empty()) or llm_interrupted_by_voice
                                         
                                         if was_jarvis_active:
-                                            cleaned_text = text.lower().strip()
+                                            text_clean = text.lower().strip()
                                             for p in [".", ",", "!", "?", "-"]:
-                                                cleaned_text = cleaned_text.replace(p, "")
-                                            cleaned_text = cleaned_text.strip()
+                                                text_clean = text_clean.replace(p, "")
+                                            text_clean = text_clean.strip()
                                             
-                                            stop_words = ("jarvis", "para", "pare", "parar", "cala a boca", "silêncio", "quieto", "stop", "shut up", "be quiet", "silence", "pera", "espera", "calma", "chega", "shh", "shush")
-                                            if any(word in cleaned_text for word in stop_words):
+                                            stop_words = ("pare", "parar", "cala a boca", "silêncio", "quieto", "stop", "shut up", "be quiet", "silence", "pera", "espera", "calma", "chega", "shh", "shush")
+                                            if any(word in text_clean for word in stop_words):
+                                                # AEC check on partial transcription to ensure Jarvis's own voice doesn't trigger false self-interruption
+                                                is_echo = False
+                                                now = time.time()
+                                                browser_recent_texts = [entry for entry in browser_recent_texts if now - entry[0] < 20.0]
+                                                
+                                                for _, spoken_text in browser_recent_texts:
+                                                    if text_clean in spoken_text or spoken_text in text_clean:
+                                                        is_echo = True
+                                                        break
+                                                    else:
+                                                        words_trans = set(text_clean.split())
+                                                        words_spok = set(spoken_text.split())
+                                                        if words_trans and words_spok:
+                                                            intersection = words_trans.intersection(words_spok)
+                                                            if len(intersection) / len(words_trans) > 0.6:
+                                                                is_echo = True
+                                                                break
+                                                
+                                                if is_echo:
+                                                    logger.debug(f"WS Audio AEC Parcial: Eco do TTS ignorado: '{text}'")
+                                                    return
+                                                    
                                                 # Interrompe geração e áudio imediatamente!
                                                 if llm_task and not llm_task.done():
                                                     llm_engine.interrupt()
@@ -504,7 +544,7 @@ async def ws_audio_endpoint(websocket: WebSocket) -> None:
                                                 llm_interrupted_by_voice = True
                                                 browser_tts_end_time = 0.0
                                                 
-                                                logger.info(f"WS Audio: Jarvis foi interrompido imediatamente ao detectar a palavra de parada '{cleaned_text}' via transcrição parcial.")
+                                                logger.info(f"WS Audio: Jarvis foi interrompido imediatamente ao detectar a palavra de parada '{text_clean}' via transcrição parcial.")
                                                 
                                                 # Limpa buffers de fala atuais
                                                 audio_buffer = []
